package profile

import (
	"testing"
)

func TestSave(t *testing.T) {
	// ctrl := gomock.NewController(t)
	// defer ctrl.Finish()

	// mockRepo := mocks.NewMockProfileRepo(ctrl)

	// profile := models.Profile{

	// }
	// mockRepo.EXPECT().Save(gomock.Any(), )
}
